{
  "name": "@louvre/umbrella",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "del 'node_modules' 'packages/*/node_modules'",
    "format": "prettier --write '**/*.{js,json,ts}' '!packages/*/lib'"
  },
  "devDependencies": {
    "del-cli": "^1.1.0",
    "prettier": "^1.14.2",
    "pretty-quick": "^2.0.1",
    "sort-package-json": "^1.15.0"
  }
}
