{
  "name": "louvre",
  "version": "0.0.2",
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "bin": {
    "louvre": "lib/bin.js"
  },
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf lib",
    "watch": "npm run clean && tsc --watch"
  },
  "dependencies": {
    "@louvre/cli": "^0.0.2",
    "@louvre/core": "^0.0.2"
  },
  "devDependencies": {
    "typescript": "^3.0.3"
  },
  "engines": {
    "node": ">=8"
  }
}
