{
  "name": "louvre",
  "version": "0.0.2",
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "bin": {
    "louvre": "lib/bin.js"
  },
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "del-cli lib",
    "watch": "npm run clean && tsc --watch"
  },
  "dependencies": {
    "@louvre/cli": "^0.0.2",
    "@louvre/core": "^0.0.2"
  },
  "devDependencies": {
    "del-cli": "^1.1.0",
    "typescript": "^3.0.3"
  },
  "engines": {
    "node": ">=8"
  }
}
